<Window x:Class="VoiceInkWin.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:VoiceInkWin.Views"
        Title="VoiceInk Settings" Height="550" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize" MinHeight="450" MinWidth="550">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="General">
                <views:GeneralSettingsView x:Name="GeneralTab"/>
            </TabItem>
            <TabItem Header="Model">
                <views:ModelSettingsView x:Name="ModelTab"/>
            </TabItem>
            <TabItem Header="AI Processing">
                <views:AISettingsView x:Name="AITab"/>
            </TabItem>
            <TabItem Header="Advanced">
                <views:AdvancedSettingsView x:Name="AdvancedTab"/>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Margin="5,0" Click="SaveButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
